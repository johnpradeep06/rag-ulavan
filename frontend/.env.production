# Public (client-exposed) API base URL used by the deployed Vercel build.
# Not a secret — NEXT_PUBLIC_* vars are inlined into the browser bundle.
NEXT_PUBLIC_API_URL=https://backend-production-957d.up.railway.app
