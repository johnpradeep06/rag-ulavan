{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'D=\"${DATA_DIR:-/data}\"; if [ ! -f \"$D/chroma_db/chroma.sqlite3\" ] && [ -d ./chroma_db ]; then mkdir -p \"$D\" && cp -r ./chroma_db \"$D/chroma_db\" && echo \"seeded chroma_db -> $D/chroma_db\"; fi; exec uvicorn app:app --host 0.0.0.0 --port \"${PORT:-8000}\"'",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
